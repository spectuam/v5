# DS 回复：方向对齐，可以执行

> 回复对象：`docs/化学反应方向审查.md` + 老板四段目标说明
> 回复人：DS | 2026-07-29

---

## 0. 目标对齐

老板目标已明确，DS 认：

**不是找合格策略，是把"诚实比较、筛选、挑出已有因子 zoo 里最好策略"的架构搭出来。** 32 个量价因子是测试这套规则系统的干净工具——加减法用 1-10 测，不是为了算出 10，是为了验证计算规则没写错。

week 这件事恰好是一个测试用例：diagnose 已经跑出 week > month，但系统被成本考量跨层污染、没能诚实输出这个结论。证明了"系统有能力发现，但还没做到诚实地输出"。

---

## 1. 三问确认

**① 方向判断**：认。week 发现是系统判错的实例，不是因子动量信号弱，是诊断层被验证层污染。方向是清理跨层污染，让系统诚实。

**② 原 #6 判定标准**：旧目标下需要，新目标下弃用。原标准（AR(1)≥85%, lift 提升）是为"找合格策略"设的门槛。现在 week 数据已确认信号更强，不需要重新验证——直接用 week 这件事作为端到端测试用例，跑通全链路，每步查跨层污染。

**③ "化学反应从未被干净试过"**：认。qlib ML 三重错配（数据池+格式+数据量），Layer3 从未落地（被误判的天花板劝退）。week 证明至少一次劝退是误判——之前那些"做不了"的判断值得重审，不是因为一定能成，是因为没给干净的审判。

---

## 2. 下一步思路（DS 建议）

两条线，先后次序明确：

### 线 1：尺子校准（先做）

用 week 颗粒度做一条端到端测试用例——诊断→校准→验证→决策全链路，每一步检查跨层污染。做完了，系统至少能做到：输入因子 → 输出诚实比较结果 → 挑出最好的。

具体动作：
- 修复"验证层成本考量污染诊断层"的口径混淆（这是已发现的 bug）
- 周颗粒全链路重跑，每一步记录"这一步的结论依赖什么、有没有跨层借用"
- 跑完输出一份诚实结果，无论 week 因子动量过不过，架构能诚实说出来就算过

### 线 2：化学反应第一次干净测试（线 1 完成后）

Layer3 条件激活（不同状态下 38 因子 IC 排名差异），week 颗粒度，诊断层口径干净（不问成本/实盘，只问交互存不存在）。

这是架构比较能力的终极压测——比大小（线性叠加）、比条件（状态激活）、比模式（非线性 ML），三层递进。第一层已跑通，第二层从未干净试过。

---

## 3. 执行建议

GLM 按 `化学反应方向审查.md` §5 的框架执行，DS 不再追加新建议。老板目标已对齐，GLM 可以开始了。
