# v5 策略升级方案：三层递进注意力架构

> 讨论: 2026-07-20 上午
> 来源: 等权排名问题 → 注意力分散 → 三层递进收窄

---

## 一、问题诊断

### 当前架构（自下而上平铺）
```
38因子 × 5000只 → 等权投票 → Top5
```

### 核心问题
1. **等权 = 没看**：38 个因子同样权重，IC 高的和低的各一票
2. **注意力分散**：5000 只票全量百分位排名，含大量僵尸票稀释信号
3. **时间维度太长**：IC 权重来自 2006-2015，市场结构已变
4. **缺乏递进结构**：不区分市场状态、不考虑行业差异

### 指导思想
- 不造轮子，先找现成方案
- 自上而下渐进收窄：大盘 → 板块 → 板块内因子 → 选股

---

## 二、目标架构

```
Layer 1: 大盘状态识别
  沪深300 日线 → HMM (3~6状态) → {牛, 熊, 震荡, ...}
  5000只
     ↓
Layer 2: 行业/板块轮动
  残差动量 or 宏观超预期打分 → 申万二级 → Top 1~3 行业
  5000 → 200~800只
     ↓
Layer 3: 板块内自适应因子
  不同(状态, 行业) → 激活不同因子组合 → 排名选股
  200 → 5只
```

### 为什么这么分
- 每层都在收窄注意力，算力投在刀刃上
- 牛市/熊市/震荡 — 有效因子完全不同
- 银行 vs 科技 — 有效因子也完全不同
- 让数据自己告诉我们：什么时候、在哪儿、用哪个因子

### 渐进式验证：回测周期也收窄
- 三层各自独立验证，不绑在一锅跑 20 年
- Layer 1 输出状态标签 → Layer 2 只在分段内验证行业轮动 → Layer 3 只在（状态 × 行业）交集窗口内验证因子
- 每层问题范围被框死，不会串味，算力递增但样本递减

---

## 三、现有轮子调研

### Layer 1: 市场状态识别 ✅ 轮子成熟

| 方案 | 技术 | 成熟度 |
|------|------|:---:|
| `hidden_regime` pip 包 | HMM, Baum-Welch 训练, Viterbi 解码 | 高，直接装直接用 |
| `hmmlearn.GaussianHMM` | 同上，更底层 | 高，A 股案例很多 |
| 知乎 A 股实操 | 沪深300, 6状态, 区分大涨/缓涨/阴跌 | 有参考代码 |

**建议起步**：沪深 300 日线 → 对数收益 + 波动率 + 成交量 → HMM 3~4 状态

### Layer 2: 行业轮动 ✅ 方法论成熟，需适配

| 方案 | 来源 | 效果 |
|------|------|:---:|
| 残差动量因子 | 华泰 2026 | 年化超额 12.9% |
| 宏观"超预期"打分 | 中信 2025 | 夏普 2.05 |
| 拥挤度预警 | 华泰 | 顶部风险信号有效 |
| 龙头扩散效应 | 广发 2025 | 年化超额 11.7% |

**我们已有的**：stock_sw2 申万二级映射（208 行业，5191 只匹配）

### Layer 3: 板块内自适应因子 ❌ 没有现成轮子

- 业内大多停在"这个行业配多少仓位"
- 没人做"在这个行业里该激活哪组因子来选股"
- 但这正是我们有积累的地方（38 正交因子 + IC 数据库 + 20 年日线）
- **需要自己写，但前两层收窄后算力可控**

---

## 四、调研补充：适合我们的方案

> 定位：私人量化，100 万量级，持仓周期短（T+1 ~ 几天）

### 回测周期与过拟合

| 要点 | 对我们意味着什么 |
|------|------|
| 每增加 1 个参数/条件，需要 **15 次以上独立交易**验证 | 我们 38 因子模型参数爆炸，样本量远不够 |
| 训练数据 < 3 年 → 实盘 3~6 个月就失效；**5 年以上才稳定** | 我们有 20 年，这个不缺 |
| **Walk-Forward Optimization (WFO)** 是验证金标准 | 我们现在只做了单次 Train/Valid/Test，不够 |
| WFE（样本外收益 / 样本内收益）应该在 **0.5~0.7** 之间 | 我们 Valid→Test 从 55% 掉到 51%，WFE≈0.92 反而说明"样本内也没多好" |
| **奥卡姆剃刀**：3 个有经济逻辑的因子 > 5 个没逻辑的 | 我们 38 个等权，太多了 |
| 小资金冲击成本小是优势，但**换手成本不能忽略**（每次 0.2%~0.9%） | 日频交易的话，一年 200 次换手可能烧掉 40% 本金 |

### 概念板块 > 传统行业

| 发现 | 数据 |
|------|:---:|
| A 股**不存在传统行业动量效应**，但**概念动量效应显著** | — |
| 传统行业分类"失真"——静态、无法反映多元业务 | — |
| 概念动量 F=6月/H=6月，经五因子调整后**月均超额 1.25%** | 年化 ~15% |
| 概念指数 AI 轮动，年化超额 **18.06%** | IR=1.73 |
| 在概念分域内做因子选股，中证 500 年化超额 **5.2%→8.8%** | IR 1.02→1.74 |
| 概念龙头识别+FCF 因子，年化超额 **20.63%** | IR=1.61 |

**结论**：我们的 Layer 2 应该以概念板块为主，申万行业作为对照组。概念分类下因子行为更趋同，分域选股提升显著。

### 个人散户量化最难的是什么

1. **换手成本**：日频策略的实际成本容易被忽略，我们做 T+1 必须建模滑点和手续费
2. **过度设计**：因子越多、规则越复杂，过拟合越快。保持极简
3. **单次验证不可信**：必须 WFO 滚动窗口，只看一次 Test 等于瞎子摸象
4. **经济逻辑 > 统计显著性**：每个因子应该能说清楚"赚的什么钱"

---

## 五、与现有 v5 的关系

| 保留 | 暂不动 | 可以改 |
|------|------|------|
| 38 正交因子池 | IC 数据库 | daily_pick 排名逻辑 |
| 20 年 TDX 日线 | Sina 日更通道 | 因子衰减扫描（加状态维度） |
| stock_sw2 行业映射 | 定时器 | 回测脚本（加行业维度） |

---

## 六、实施计划与资源配置

### 总览

| Phase | 内容 | 工作量 | 内存需求 | 是否需升级 |
|:---:|------|:---:|:---:|:---:|
| 0 | 验证市场状态可分 | 30min 跑数 + 看结果 | <500MB | ❌ 不需要 |
| 1 | Layer 1 状态识别上线 | 半天 | <100MB/日 | ❌ 不需要 |
| 2 | Layer 2 概念数据 + 轮动 | 2~3 天（数据源调研+复现） | <1GB | ❌ 不需要 |
| 3 | Layer 3 板块内自适应因子 | 3~5 天（核心攻坚） | 1~3GB | ⚠️ 可能需要 |
| 4 | 验证(WFO) + v6 集成 | 2~3 天 | 1~3GB | ⚠️ 可能需要 |

### Phase 0: 验证市场状态可分

**目标**：跑一组数据，看一眼，决定要不要继续。最轻量，零风险。

**步骤**：
1. 从 tdx_stock_data.db 取沪深 300 指数日线（或自己算等权指数）
2. pip install hidden-regime，默认参数跑 HMM，看分出几个状态
3. 每状态标注后，取对应日期范围内 38 因子 IC 分别统计
4. 每状态下各申万行业收益分布
5. 输出一张表：各状态下 IC 均值最高的 5 个因子 + 收益最高的 3 个行业

**判断标准**：
- ✅ 不同状态下因子 IC 排名差异明显 → 值得继续
- ❌ 各状态因子 IC 都差不多 → 说明"大盘状态不影响因子有效性"，Layer 1 没意义，直接跳到概念分域

**资源**：
- 计算：沪深 300 日线 ~5000 天，HMM 训练 <1 秒，因子 IC 统计复用已有数据
- 内存：<500MB，当前 10G 完全够

### Phase 1: Layer 1 — 市场状态识别

**目标**：把市场状态变成每日自动输出的标签，喂给下游。

**步骤**：
1. 确定方案：`hidden_regime`（快速上手）vs 裸 `hmmlearn`（灵活控制特征）
2. 确定特征：对数收益 + 滚动波动率 + 成交量变化（3~5 个，坚持奥卡姆剃刀）
3. 确定状态数：AIC/BIC 扫描 + 人眼判断可解释性（3 还是 4，坚决不超过 5）
4. 写 `market_regime.py`：读日线 → 滚动训练 HMM → 输出今天的状态标签 + 各状态概率
5. 回测：不同状态下 38 因子等权的 WR，确认 Layer 1 本身有增量信息
6. 加入定时器：每天收盘后更新状态

**资源**：
- 日频运行：读指数日线 + predict，<1 秒，<100MB
- 周末滚动重训 HMM（可选）：5 分钟，<200MB

### Phase 2: Layer 2 — 概念板块数据 + 轮动

**目标**：拿下概念板块数据，验证"概念 > 行业"，选一套轮动方案。

**步骤**：
1. **数据源调研**：
   - TDX `/vipdoc` 板块文件（本地，离线，但格式需解析）
   - 东方财富概念板块 API（在线，JSON，方便）
   - 同花顺 iFinD（可能需要权限）
   - **选最省事的，不纠结**
2. **入库**：概念 → 成分股映射表，参考 stock_sw2 的格式
3. **对比验证**：同一时间段，申万行业分域 vs 概念分域，38 因子 IC 在哪种分域下组内一致性更高
4. **轮动方案**（选一个，不做三选一的对比实验）：
   - 优先尝试残差动量（最简单，有公开实现参考）
   - 如果效果不好再换超预期打分
5. 回测：概念轮动选出的 Top3 概念 × 全量 38 因子 vs 当前全市场版本

**资源**：
- 概念数据拉取：一次性，几十 MB
- 回测：类似现有 line_bcd_backtest，跑一次 20~40 分钟，<1GB

### Phase 3: Layer 3 — 概念板块内自适应因子

**目标**：每个（状态 × 概念）找到自己最好的因子。这是核心攻坚，也是我们跟卖方最大的区别——他们停在行业配置，我们往下挖到选股。

**步骤**：
1. 从 factor_ic_daily 表，按（日期范围, 概念成分股）分组，统计各因子在各组内的 T1_IC
2. 合并状态标签，变成 (状态, 概念, 因子) → IC 均值表
3. 筛掉样本量太小的组（比如某个概念在某个状态下只有 <30 个交易日）
4. 对每个组，选 IC 最高的 3~5 个因子（不是 38 个全用）
5. 验证：这些"自适应因子组合"在各组样本外区间的 IC 是否明显高于 38 全量等权
6. 如果 Pattern 稳定，写规则；如果不稳定 → 按周/月滚动更新因子-概念映射表

**资源**：
- 计算量取决于（状态 × 概念）的组数。假设 4 状态 × 50 个有数据的概念 = 200 组
- 但每组只算 38 个因子的 IC 均值，从 factor_ic_daily 查表，不重新算因子值
- 查询量：200 组 × 38 因子 = 7600 次轻量 SQL 聚合，几分钟
- 内存：<1GB（只做统计，不跑全量因子计算）
- **不需要内存升级**

### Phase 4: 验证与集成

**目标**：用 WFO 替代单次回测，加入成本建模，推出 v6。

**步骤**：
1. **WFO 框架**：3 年滚动训练窗口，1 年测试窗，滚动前进
2. **换手成本**：扫描 0.1%/0.2%/0.5% 三档，看策略在哪档开始亏钱
3. **daily_pick_v6.py**：
   - 读取今日市场状态 → 读取今日强势概念 → 激活对应因子组合 → 在概念成分股内排名 → Top5 → 飞书
4. 周末扫描加入状态维度和概念维度
5. 对比报告：v5（38 因子等权）vs v6（三层递进）各周期 WR

**资源**：
- WFO 回测：周末跑，3~5 小时，内存取决于面板大小（分窗口可控制）
- 日频 daily_pick：比 v5 更轻（因子数从 38 → 3~5，选股池从 5000 → 200~800），<30 秒

---

## 七、内存升级决策

| 场景 | 10G 够吗 | 说明 |
|------|:---:|------|
| Phase 0~2 | ✅ 够 | 轻量，数据量小 |
| Phase 3 IC 统计 | ✅ 够 | 查表聚合，不跑因子计算 |
| Phase 4 WFO 回测 | ⚠️ 勉强 | 分窗口跑可以控制，但全量跑可能吃紧 |
| 周末 factor_decay_scan（已有） | ⚠️ 勉强 | 456 因子 × 20 年面板，磁盘辅助已解决 |
| Line E (Qlib ML) | ❌ 不够 | 8.7G RSS，等升级 |
| 未来概念自适应全量扫描 | ⚠️ 看怎么切 | 按概念切片跑，每个切片小 |

**结论**：10G 能走完 Phase 0~4 全流程，不需要等内存升级再开工。Line E 和未来的 RD-Agent 自动因子挖掘才需要 32G。

---

## 八、关键决策待定

1. HMM 状态数取几个？（3 简单 vs 4~6 更细但难解释）
2. HMM 观测特征用什么？（价量 vs 宏观经济 vs 市场指标）
3. 行业轮动方案选哪个？（残差动量 vs 超预期 vs 自己做）
4. Layer 3 做到什么粒度？（每个行业一套 vs 行业聚类后用几套）

---

## 九、风险提示

- **过拟合风险**：HMM 状态数越多越容易过拟合历史
- **数据量要求**：行业层面样本量远小于全市场，Layer 3 容易噪声大
- **渐进推进**：每层验证完再进下一层，不要一上来就三层全做
- **复杂度陷阱**：38 因子已经够多，不要再加。问题不是因子不够，是用不对地方
- **概念数据来源**：东方财富 API 可能不稳定，TDX 本地文件更可靠但格式需解析，先调研再选
- **10G 够用**：全流程不需要等内存升级，先跑起来
